services:
  db:
    image: postgres:15
    container_name: petconnect-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: petconnect
    ports:
      - "5432:5432"
